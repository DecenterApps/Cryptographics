const ipfsNodePath = 'https://ipfs.decenter.com/ipfs/';

const DEFAULT_AVATAR = 'default-avatar';
const DEFAULT_USERNAME = 'Anon';

module.exports = {
  ipfsNodePath,
  DEFAULT_AVATAR,
  DEFAULT_USERNAME,
};
